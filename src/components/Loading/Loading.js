import './loading.css';

const Loading = () => {
    return(
  <div id='loading'>
      <i class="fas fa-sync fa-spin" id='loading-icon'></i>
    <p>Hang up here, we're almost loaded.</p>
  </div>
    )
}

export default Loading;